# Server port
PORT=5000

# Production: set to "production" when deploying
# NODE_ENV=production

# Comma-separated list of allowed origins for CORS (required in production if frontend is on another domain)
# Example: https://myapp.com,https://www.myapp.com
# ALLOWED_ORIGINS=

# Zego Cloud - get from https://console.zegocloud.com
# Server Secret must be exactly 32 characters
ZEGO_APP_ID=your_app_id_number
ZEGO_SERVER_SECRET=your_32_character_server_secret
