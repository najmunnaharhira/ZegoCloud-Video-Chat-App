# Backend API URL (only needed when frontend is served from a different origin)
# Leave empty when using Vite proxy in development or when frontend and backend are on same host
# Example for production: https://api.myapp.com
# VITE_API_URL=

# Development-only: Zego credentials for client-side token (not for production)
# For production, use server-side token via ZEGO_APP_ID and ZEGO_SERVER_SECRET in server/.env
# VITE_ZEGO_APP_ID=
# VITE_ZEGO_SERVER_SECRET=
